(define (every f l)
	(if (empty? l)
		'()
		(se (f (first l)) (every f (bf l)))
	)
)

(define (keep pred? l)
	(if (empty? l)
		'()
		(if (pred? (first l))
			(se (first l) (keep pred? (bf l)))
			(keep pred? (bf l))
		)
	)
)
